import { Button, Flex } from "@chakra-ui/react";

const Commands = () => {
  return (
    <Flex>
      <Button />
      <Button />
      <Button />
      <Button />
    </Flex>
  );
};
export default Commands;
